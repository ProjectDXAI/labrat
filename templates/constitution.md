# Deprecated

`constitution.md` is no longer the authoritative scoring surface in labrat vNext.

Use:

- `evaluation.yaml` for canonical search / selection / final evaluation
- `runtime.yaml` for funding, worker pools, checkpointing, and plateau logic
