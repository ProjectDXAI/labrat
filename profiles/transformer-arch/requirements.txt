# Base lab deps (pyyaml and numpy come from the parent labrat repo's requirements.txt).

# For training.mode: "real" — install these in your lab venv when you are ready
# to move off synthetic training. Synthetic mode does not need torch.
# torch>=2.0
