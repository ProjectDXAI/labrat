# The transformer-arch profile's default runner is synthetic and uses only the
# standard library + pyyaml + numpy that already come from the top-level
# labrat requirements.txt. No profile-specific deps required.

# When you swap in a real trainer (e.g. PyTorch, JAX), add its deps here.
