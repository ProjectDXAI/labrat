# Phase 0: Deep Research And Tree Design

This example already includes a completed Phase 0. Use `research_brief.md`, `research_sources.md`, and `branches.yaml` as the reference outputs.

If the human wants to redesign the tree, read `tree_designer.md` and regenerate the Phase 0 artifacts before touching bootstrap or cycle execution.

When redesigning, keep the full search ladder explicit:

- cheap probes
- normal exploitation
- implementation audit
- formulation change
