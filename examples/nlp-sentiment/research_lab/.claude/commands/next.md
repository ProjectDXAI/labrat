Run `python scripts/operator_helper.py next-prompt --runner claude --phase auto` from this lab's root directory, read the prompt it prints, and then execute that prompt's instructions.

Stay inside this lab. Do not reach outside the lab root for context.
